<?php
/**
 * @var \Application\Entity\Tracker $tracker
 */
?>
<tr>
    <td>
        <a href="<?=$this->url('trackers/info', ['id' => $tracker->getId()]); ?>" class="color-blue">
            <?= $tracker->getFullName(); ?>
        </a>
    </td>
    <td><?= $tracker->getInn(); ?></td>
    <td><?= $tracker->getKpp(); ?></td>
    <td><?= $tracker->getPhone(); ?></td>
    <td><?= $tracker->getContactPerson(); ?></td>
    <td><?= $tracker->getDateCreated()->format('d.m.Y'); ?></td>
    <td>
        <button type="button" class="btn btn-link btn-secondary bmd-btn-fab-sm contragentEditBtn"
                data-get-url="<?=$this->url('trackers/get', ['id' => $tracker->getId()]); ?>"
        >
            <i class="material-icons">mode_edit</i>
        </button>
    </td>
</tr>
